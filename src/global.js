export const PARENT_HEIGHT = document.documentElement.clientHeight;
export const PARENT_WIDTH = document.documentElement.clientWidth;

export const PARENT_TOP = document.documentElement.clientTop;
export const PARENT_LEFT = document.documentElement.clientLeft;
